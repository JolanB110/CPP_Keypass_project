#include "Application.hpp"

/*Application::Application(const std::string& name, const std::string& type)
    : name(name), type(type), mail(""), path_to_user("") {}

EmailApplication::EmailApplication(const std::string& name, const std::string& type,
                                   const std::string& mail)
    : Application(name, type), mailAddress(mail) {}

BankApplication::BankApplication(const std::string& name, const std::string& type,
                                 int cardNum, int cvv, const std::string& expDate)
    : Application(name, type), cardNumber(cardNum), cvv(cvv), expirationDate(expDate) {}*/